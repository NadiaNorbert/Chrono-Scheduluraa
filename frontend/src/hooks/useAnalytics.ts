"use client";

/**
 * [FUTURE M4 — Analytics] Analytics data hook stub.
 * Wire up to GET /api/v1/analytics/report in Milestone 4.
 */
export function useAnalytics() {
  return {
    report: null,
    isLoading: false,
  };
}

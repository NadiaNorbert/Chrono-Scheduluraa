"use client";

/**
 * [FUTURE M3 — AI] AI suggestion hook stub.
 * Wire up to GET /api/v1/scheduler/suggestions in Milestone 3.
 */
export function useAI() {
  return {
    suggestions: [],
    isLoading: false,
  };
}
